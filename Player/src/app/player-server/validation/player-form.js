/*
var Joi = require('joi');

module.exports =  {
    body: {
        firstname:  Joi.string().regex(/[a-zA-Z]{3,16}/).required(),
        lastname: Joi.string().regex(/[a-zA-Z]{3,16}/).required(),
        age: Joi.number().integer().required(),
        position: Joi.string().regex(/[a-zA-Z ]{3,30}/).required(),
        skill: Joi.string().regex(/[a-zA-Z ]{3,30}/).required(),
        language_level: Joi.string().regex(/^(A|B|C)(1|2)\+?$/).required(),
        experience: Joi.number().integer().required()
    }
};*/
